export function createCountString(waterfrontCount, skylineCount, castleCount) {
    return `The waterfront has been selected ${waterfrontCount} times,
    the skyline has been selected ${skylineCount} times,
    and the castle has been selected ${castleCount} times.`;
}